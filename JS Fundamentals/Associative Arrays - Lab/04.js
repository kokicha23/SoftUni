function solve(input) {
	// let assocArr = {};
	// for (let entry of input) {
	// 	entry = entry.split(' ');
	// 	let product = entry[0];
	// 	let quantity = Number(entry[1]);
	// 	if (assocArr.hasOwnProperty(product)) {
	// 		assocArr[product] = assocArr[product] += quantity;
	// 	} else {
	// 		assocArr[product] = quantity;
	// 	}
	// }
	// for (let assocKey in assocArr) {
	// 	console.log(`${assocKey} -> ${assocArr[assocKey]}`);
	// }
}

solve(['tomatoes 10', 'coffee 5', 'olives 100', 'coffee 40']);
solve(['apple 50', 'apple 61', 'coffee 115', 'coffee 40']);
