function solve(name,population,treasury) {
    let city ={
        "name": name,
        "population": population,
        "treasury":treasury
    }
    console.log(city[name])
    console.log("\n")
}
solve ('Tortuga',
7000,
15000)